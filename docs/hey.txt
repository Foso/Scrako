private class REPLACE_NAME(INSERT_PARAMETER INSERT_EFFECT) : Node {
    override fun visit(
        visitors: MutableMap<String, Block>,
        parent: String?,
        identifier: String,
        nextUUID: String?,
        context: Context) {
        REPLACE_BLOCKO
        visitors[identifier] = BlockSpec(
            opcode = REPLACE_OPCODE,
            inputs = mapOf(
                REPLACE_INPUT
            ),
            fields = mapOf(
                REPLACE_FIELDS
            )
        ).toBlock(nextUUID, parent)
        HERE
    }
}