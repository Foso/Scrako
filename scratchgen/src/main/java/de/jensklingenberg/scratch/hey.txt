private class REPLACE_NAME(INSERT_PARAMETER, INSERT_EFFECT) : Node {
    override fun visit(
        visitors: MutableMap<String, Block>,
        parent: String?,
        identifier: UUID,
        nextUUID: UUID?,
        context: Context
    ) {
        val blockId = UUID.randomUUID()
        visitors[identifier.toString()] = BlockSpec(
            opcode = REPLACE_OPCODE,
            inputs = mapOf(
                REPLACE_INPUT
            ),
            fields = mapOf(
                REPLACE_FIELDS
            )
        ).toBlock(nextUUID, parent, context.topLevel)
        block.visit(visitors, identifier.toString(), blockId, null, context)
    }
}