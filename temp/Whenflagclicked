private class Whenflagclicked( ) : Node {
    override fun visit(
        visitors: MutableMap<String, Block>,
        parent: String?,
        identifier: UUID,
        nextUUID: UUID?,

    ) {
        
        visitors[identifier.toString()] = BlockSpec(
            opcode = "event_whenflagclicked",
            inputs = mapOf(
                
            ),
            fields = mapOf(
                
            )
        ).toBlock(nextUUID, parent)
        
    }
}