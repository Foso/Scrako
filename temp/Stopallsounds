private class Stopallsounds( ) : Node {
    override fun visit(
        visitors: MutableMap<String, Block>,
        parent: String?,
        identifier: UUID,
        nextUUID: UUID?,

    ) {
        
        visitors[identifier.toString()] = BlockSpec(
            opcode = OpCode.sound_stopallsounds,
            inputs = mapOf(
                
            ),
            fields = mapOf(
                
            )
        ).toBlock(nextUUID, parent)
        
    }
}